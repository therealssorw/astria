# Godot 4+ specific ignores
.godot/
/android/

# Exported binaries (rebuild with --headless --export-release)
/build/

# Claude Code local session artifacts
/.claude/scheduled_tasks.lock

# Discord webhook URL — a secret, and this repo is public. Read at runtime by
# scripts/core/discord_notifier.gd; put one beside the server binary too.
/discord_webhook.txt
